




<div class="row text-center"> 
    <h2 class="jumbotron alert-success"> 네이버 Trending Chat </h2>
</div>

<div class="row">


    <% if logged_in? %>

            <div class="col-sm-12"> 
                <div class="btn btn-default"> <%= link_to 'Home', root_path %> </div>
                <span class="text-right"> &nbsp; Hello, <strong> <%= current_user.name %> </strong> | </spa>
                <%= link_to 'Logout', logout_path %>
            </div>

    <% else %>

            <div class="col-sm-4">
                <%= form_for(:session, url: sessions_path) do |f| %>
                        <div class="input-group">
                            <%= f.text_field :name, class: 'form-control' %>
                            <span class="input-group-btn">
                                <%= f.submit "Log in", class: "btn btn-default" %>
                            </span>
                        </div>
                <% end %>
             </div>


                <div class="col-sm-2 border">
                    <button class="btn btn-large"> <%= link_to 'Sign up', new_user_path %> </button>
                </div>
                

            
    <% end %>

</div>